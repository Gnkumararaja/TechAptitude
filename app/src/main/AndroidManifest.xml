<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.kumararaja.techaptitude">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".CategoryTest"></activity>
        <activity android:name=".SplashscreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".QuestionSets" />
        <activity android:name=".VerbalQuestions" />
        <activity android:name=".NumericQuestions" />
        <activity android:name=".AnalyticalQuestions" />
        <activity android:name=".TesttypeActivity" />
        <activity android:name=".ProfileActivity" />
        <activity android:name=".ForgetPassword" />
        <activity android:name=".CategoryClass" />
        <activity android:name=".MainActivity"></activity>
        <activity android:name=".Homepage" />
    </application>

</manifest>